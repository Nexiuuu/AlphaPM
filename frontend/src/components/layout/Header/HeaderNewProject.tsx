import { Plus } from "lucide-react";

import { Button } from "../../ui/Button/Button";

export const HeaderNewProject = () => {
  return (
    <Button>
      <Plus size={18} />
      New Project
    </Button>
  );
};